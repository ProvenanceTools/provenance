# Provenance Heuristics

The Provenance Analyzer's heuristics suite reads a sealed `.slog` bundle and produces a list of `Flag`s shown in the Submission Overview and Replay views. Every flag is a sort-priority signal for staff triage — never a verdict. The replay UI is the verification path; clicking any flag jumps to the supporting event in context.

For the overall product, see the [repo root README](../README.md). For the full spec, see [`prd.md`](./prd.md). For the analyzer build plan, see [`analyzer-implementation-plan.md`](./analyzer-implementation-plan.md).

## How heuristics are run

A single entry point — [`runHeuristics`](../packages/analysis-core/src/heuristics/run-heuristics.ts) — iterates a fixed registry, collects all flags, appends [integrity flags](../packages/analysis-core/src/heuristics/integrity-flags.ts) derived from the bundle validation report (PRD §5.4), and sorts:

1. severity descending (`high` → `medium` → `low` → `info`)
2. confidence descending
3. supporting seq key ascending (stable tie-break — deterministic across runs and snapshot-test-friendly)

Cross-submission heuristics run separately via [`runCrossHeuristics`](../packages/analysis-core/src/heuristics/cross/run-cross-heuristics.ts), only when more than one bundle is loaded into the `/compare` view.

Configuration defaults live in [`config.ts`](../packages/analysis-core/src/heuristics/config.ts). Course staff can override per-heuristic via the `HeuristicConfig` shape passed to `runHeuristics`.

Severity / confidence semantics:

- **Severity** is the heuristic's claim about how serious the signal is in isolation: `high`, `medium`, `low`, `info`. `info` is informational context (e.g. "an AI extension was active"), not an integrity claim by itself.
- **Confidence** is how reliable the signal is on this particular bundle (0.0–1.0). A high-severity flag with 0.6 confidence is "loud signal, less sure"; a medium with 0.95 confidence is "subtle but reliable". Both are surfaced; staff decide weighting.

## Capture policy and the absence-vs-disabled rule

From Manifest 2.0 on, a course can turn individual capture signals down via the
signed `policy.capture` block (program spec
[§4](./superpowers/specs/2026-08-18-multicourse-program-architecture.md)):
`selection_change`, `focus_change`, `terminal`, and the
`heartbeat_interval_ms` cadence. The effective policy
travels into the bundle inside `session.start.data.manifest`, so the analyzer
can tell "this student produced no `terminal.open` events" from "this course
disabled terminal capture".

**Any heuristic that consumes a policy-gated signal returns _not-applicable_
(zero flags) when the course disabled it** — never a flag, never a zero. The
signed policy decides, not what happens to be in the event stream, so a
hand-edited log cannot re-enable a heuristic the course turned off.

| Heuristic                                | Gated on                | Behaviour when disabled                                     |
| ---------------------------------------- | ----------------------- | ----------------------------------------------------------- |
| `no_intermediate_errors`                 | `terminal`              | no flags                                                    |
| `shell_integration_disabled`             | `terminal`              | no flags                                                    |
| `terminal_active_during_external_change` | `terminal`              | no flags                                                    |
| `gap_in_heartbeats`                      | `heartbeat_interval_ms` | threshold becomes `max(configured, 10 x recorded interval)` |
| `editing_pattern_clone` (cross)          | any kind-stream signal  | pair skipped — a shrunken kind alphabet inflates Jaccard    |

Every 1.0/1.1 bundle resolves to the default policy (everything on, 30 s
heartbeat), so archived submissions score exactly as they always did.

Paste and external-change **content** is on the hard floor too. There was
briefly an `inline_content` key that stripped the `content` / `new_content`
snippets while leaving the events, and it was removed because it worked in the
one direction a knob must not: `internal_move` matches a paste against the
student's own prior typed code and a match **downgrades** `large_paste`, so
stripping the content disables an _exculpatory_ check and leaves genuine
self-relocations at full severity. `large_paste`, `paste_is_solution`,
`paste_matches_known_source`, and `paste_shared_across_students` are therefore
ungated: the only thing that withholds paste content from them is the recorder's
64 KB inline size cap, which is a payload guard, not a policy.

`doc.open` and `doc.close` are on the hard floor and have no `policy.capture`
key, so **no heuristic reading them is gated**. `time_to_first_save_anomaly` and
`inter_session_external_change` both anchor on `doc.open` and both run
unconditionally. `low_typing_high_output` reads `doc.open` too and likewise is
not gated: the `doc.open` content is both its `startLength` anchor and the
reconstruction seed, so its absence cancels out of the ratio instead of
inflating it.

The audit lives in
[`policy-gating.test.ts`](../packages/analysis-core/src/heuristics/policy-gating.test.ts).

## Process-shape heuristics (Phases 4, 16)

These detect AI-assistance patterns inside a single submission.

| Heuristic                       | Severity                                                 | Detects                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Defaults                                                                           | Source                                                                                                                                                                                                    |
| ------------------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `large_paste`                   | medium / high                                            | Paste-shaped insertion ≥ threshold chars or lines. Covers both clipboard pastes (`paste` events) and AI-assistant "Apply" bulk edits (`doc.change` with `source: "paste_likely"`, recorder v1.2+). High at ≥ 500 chars or ≥ 30 lines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `minChars: 200`, `minLines: 10`, `highSeverityChars: 500`, `highSeverityLines: 30` | [`large-paste.ts`](../packages/analysis-core/src/heuristics/large-paste.ts) · [tests](../packages/analysis-core/src/heuristics/large-paste.test.ts)                                                       |
| `paste_is_solution`             | high                                                     | Pasted block accounts for ≥ 80% of the file's final saved state, and ≥ 10 lines in absolute terms — i.e. the submitted file **is** the pasted block. Gates on **coverage** (`sharedLines / finalFileLines`), deliberately **not** survival (`sharedLines / pasteLines`): survival is trivially 1.0 for any small paste nobody deleted, so a 3-line import block pasted onto a 60-line hand-typed file used to score 1.0 and raise `high`. Survival is still reported in `detail.survivalRatio` — informative, never load-bearing. The `minSharedLines` floor mirrors `largePaste.minLines`: `large_paste` will not raise even `medium` below 10 lines, and this flag raises `high`. Uses [`reconstructFileWithProvenance`](../packages/analysis-core/src/index/reconstruct-file-provenance.ts) for the comparison.                                                                             | `finalFileCoverage: 0.8`, `minSharedLines: 10`                                     | [`paste-is-solution.ts`](../packages/analysis-core/src/heuristics/paste-is-solution.ts) · [tests](../packages/analysis-core/src/heuristics/paste-is-solution.test.ts)                                     |
| `external_edits`                | medium / high                                            | `fs.external_change` event with no preceding formatter/git event — a CLI tool wrote to the file directly. Coalesces same-file events within 2 s.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `coalesceWindowMs: 2000`, `highSeverityCharsChanged: 100`                          | [`external-edits.ts`](../packages/analysis-core/src/heuristics/external-edits.ts) · [tests](../packages/analysis-core/src/heuristics/external-edits.test.ts)                                              |
| `inter_session_external_change` | medium / high                                            | File content that diverged between the END of one recorder session and the START of the next, when no recorder was running to catch it as `fs.external_change`. Compares the reconstructed content at the end of session A against session B's first `doc.open` content for the same file (recorder v1.1+). High if the length delta exceeds `highSeverityCharsChanged`, else medium. Confidence 0.85.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `highSeverityCharsChanged: 100`                                                    | [`inter-session-external-change.ts`](../packages/analysis-core/src/heuristics/inter-session-external-change.ts) · [tests](../packages/analysis-core/src/heuristics/inter-session-external-change.test.ts) |
| `mass_external_replacement`     | high                                                     | Whole-file replacement outside VS Code: new content shares < 20% lines with old.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `sharedThreshold: 0.2`                                                             | [`mass-external-replacement.ts`](../packages/analysis-core/src/heuristics/mass-external-replacement.ts) · [tests](../packages/analysis-core/src/heuristics/mass-external-replacement.test.ts)             |
| `low_typing_high_output`        | medium / high                                            | Net chars added (final − starting) divided by chars typed via `doc.change` exceeds the ratio threshold. Uses **net delta** from the file's `doc.open.content` (recorder v1.1+) so opening a non-trivial skeleton doesn't trigger a false flag. Excludes `doc.change` deltas whose `source` is `paste_likely`/`paste_confirmed` from the denominator. **Tier 3.1:** characters a `git_merge_in` delivered are subtracted from the numerator, so an honest `git pull` is not counted as this student's output.                                                                                                                                                                                                                                                                                                                                                                                   | `minRatio: 3`, `highRatio: 5`, `minCharsForConfidence: 500`                        | [`low-typing-high-output.ts`](../packages/analysis-core/src/heuristics/low-typing-high-output.ts) · [tests](../packages/analysis-core/src/heuristics/low-typing-high-output.test.ts)                      |
| `time_to_first_save_anomaly`    | high                                                     | File went from `doc.open` to a save containing > `minChars` of new code in under `anomalySeconds`. **Tier 3.1:** characters a `git_merge_in` delivered inside that window are not counted as written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `anomalySeconds: 30`, `minChars: 500`                                              | [`time-to-first-save-anomaly.ts`](../packages/analysis-core/src/heuristics/time-to-first-save-anomaly.ts) · [tests](../packages/analysis-core/src/heuristics/time-to-first-save-anomaly.test.ts)          |
| `idle_then_complete`            | high                                                     | Long idle window followed by a single save bringing the file from skeleton to complete. **Tier 3.1:** both character counts exclude what a `git_merge_in` delivered, so a pull over lunch is not a completion.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `idleGapMs: 600_000`, `sizeRatio: 0.5`, `postIdleWindowMs: 60_000`                 | [`idle-then-complete.ts`](../packages/analysis-core/src/heuristics/idle-then-complete.ts) · [tests](../packages/analysis-core/src/heuristics/idle-then-complete.test.ts)                                  |
| `no_intermediate_errors`        | medium (degrades to info when shell integration was off) | No terminal command in the **whole submission** reported a non-zero exit, over at least `minCommandsWithExitCode` commands that actually reported one. Scope is the submission, not the session: a failure in session 1 means the error-and-fix cycle happened however clean session 2 is. Commands with no recorded `exit_code` count as neither success nor failure and not toward the floor — absence of capture is not evidence of success. Any session with shell integration off suppresses the flag entirely (errors there would have been invisible) and emits the `info` degradation instead. **What it does and does not support:** "the student never saw an error" fits _pasted working code_ and _a strong programmer_ equally, and the flag does not distinguish a test run from an `ls`. Read it as "no failure was observed over N runs", never as "the code was not written". | `minCommandsWithExitCode: 5`                                                       | [`no-intermediate-errors.ts`](../packages/analysis-core/src/heuristics/no-intermediate-errors.ts) · [tests](../packages/analysis-core/src/heuristics/no-intermediate-errors.test.ts)                      |
| `paste_matches_known_source`    | medium / high                                            | **Currently inert — see note below.** Pasted text exact-hash-matches a course-maintained corpus (high) or fuzzy-line-matches it (medium). Inspects both `paste` events and `paste_likely` `doc.change` deltas.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `fuzzyThreshold: 0.7`, `corpus: []`                                                | [`paste-matches-known-source.ts`](../packages/analysis-core/src/heuristics/paste-matches-known-source.ts) · [tests](../packages/analysis-core/src/heuristics/paste-matches-known-source.test.ts)          |

**Shared iterator.** The three paste-content heuristics (`large_paste`, `paste_is_solution`, `paste_matches_known_source`) iterate paste-shaped events through one helper, [`candidate-pastes.ts`](../packages/analysis-core/src/heuristics/candidate-pastes.ts). The helper unifies native `paste` events and recorder-v1.2 `doc.change` events with `source: "paste_likely" | "paste_confirmed"`, yielding one `CandidatePaste` per inserted blob. When a new paste source is introduced (drag-and-drop file insertion, snippet expansion, etc.) only this file needs updating.

**Internal-move downgrade.** `large_paste` and `paste_is_solution` consult [`internal-move.ts`](../packages/analysis-core/src/heuristics/internal-move.ts) before emitting. When a paste's content matches a region of the student's own prior content whose provenance is _typed_ (or preexisting starter code), the flag is retitled "Code moved within/from …", dropped to `info` severity — which scores 0 under the default severity weights, so it leaves the ranked queue — and carries a `detail.internalMove` block naming the source path and event. The flag is never suppressed: the record and its audit trail remain, so a grader can jump to `sourceGlobalIdx` and see where the code was.

This exists because copying a block within a file, cutting and re-pasting it while reorganising, and moving a helper into another file are all ordinary student behaviour that previously fired at high severity. Flags that fire on normal work train graders to dismiss the flag class wholesale, which costs the true positives too.

The provenance requirement is load-bearing, not a refinement. Relocating code that itself arrived by paste or external change does **not** qualify, so a paste cannot be laundered by moving it between files. Matching is near-exact (`minMatchRatio: 0.95`) on lines with indentation stripped, so reindent-on-paste still matches while "vaguely similar" does not. Cut-then-paste is covered by a deletion ledger built from snapshots taken during the same replay pass. Everything is fail-closed: an over-cap paste with no inline content, a tainted reconstruction, or a match whose source region is not predominantly the student's own all leave the flag at full severity. Set `internalMove.enabled: false` to restore the prior behaviour exactly.

`paste_matches_known_source` is deliberately **not** downgraded — a corpus hash match is a hard signal that does not soften based on where the bytes were sitting a minute earlier.

**`paste_matches_known_source` is inert pending a corpus source.** The heuristic and its corpus loader/validator (`loadKnownSourceCorpus`) are fully implemented and tested, but nothing populates `pasteMatchesKnownSource.corpus` in a deployed system: there is no upload path, no staff-facing config field, and no storage for corpus entries anywhere in `packages/server/src` or `packages/analyzer/src`. The default is `corpus: []`, and an empty corpus makes the heuristic emit exactly 0 flags — always, regardless of its `enabled`/`weight` setting. Building corpus ingestion is a separate, deliberately out-of-scope feature. Until it ships, the analyzer's tuning UI (`TuningView.tsx`) shows this flag with both its weight slider and its enable toggle disabled, with an inline explanation, so staff cannot be misled into thinking either control does anything. The flag stays registered in `ALL_FLAG_IDS` and counted among the 29 known heuristics — removing it would understate what the engine can do once a corpus exists, and a future corpus feature only needs to populate the config, not rebuild the matcher.

## Environment heuristics (Phase 17)

Informational signals about the student's setup. By themselves these are context, not integrity claims.

| Heuristic                                | Severity | Detects                                                                                                                                                                                                                                      | Source                                                                                                                                                                                                                               |
| ---------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ai_extension_active`                    | info     | A known AI-assistant extension (Copilot, Codeium, Continue, Cursor, etc.) was enabled during the session. The known-AI list is in [`config/ai-extension-list.json`](../packages/analysis-core/src/heuristics/config/ai-extension-list.json). | [`ai-extension-active.ts`](../packages/analysis-core/src/heuristics/ai-extension-active.ts) · [tests](../packages/analysis-core/src/heuristics/ai-extension-active.test.ts)                                                          |
| `shell_integration_disabled`             | info     | VS Code shell integration was off during a terminal session, so command lines and exit codes weren't captured. Raises the prior on any terminal-adjacent flag.                                                                               | [`shell-integration-disabled.ts`](../packages/analysis-core/src/heuristics/shell-integration-disabled.ts) · [tests](../packages/analysis-core/src/heuristics/shell-integration-disabled.test.ts)                                     |
| `extension_set_changed_mid_assignment`   | medium   | An AI extension was installed or enabled while the assignment was in progress (`ext.activate` event mid-session).                                                                                                                            | [`extension-set-changed-mid-assignment.ts`](../packages/analysis-core/src/heuristics/extension-set-changed-mid-assignment.ts) · [tests](../packages/analysis-core/src/heuristics/extension-set-changed-mid-assignment.test.ts)       |
| `terminal_active_during_external_change` | info     | A terminal was open at the moment a file changed outside VS Code, suggesting a CLI tool was the source of the change.                                                                                                                        | [`terminal-active-during-external-change.ts`](../packages/analysis-core/src/heuristics/terminal-active-during-external-change.ts) · [tests](../packages/analysis-core/src/heuristics/terminal-active-during-external-change.test.ts) |

## Integrity heuristics (Phase 17)

Log-structure anomalies that suggest tampering or recorder failure.

| Heuristic                   | Severity | Detects                                                                                                                                                                                                                                                                                                                                                                                                                | Source                                                                                                                                                                                        |
| --------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `clock_jumps`               | medium   | Multiple `clock.skew` events or one large jump (> 5 min). Could indicate the recorder was paused or the system clock was adjusted.                                                                                                                                                                                                                                                                                     | [`clock-jumps.ts`](../packages/analysis-core/src/heuristics/clock-jumps.ts) · [tests](../packages/analysis-core/src/heuristics/clock-jumps.test.ts)                                           |
| `gap_in_heartbeats`         | medium   | Heartbeat gap > 5 min with no `session.end` / `session.start` between them, AND at least one other event recorded during the gap (proof the recorder process was executing). Gaps with zero intervening events of any kind are machine sleep (the extension host is suspended, not deactivated) and are not flagged.                                                                                                   | [`gap-in-heartbeats.ts`](../packages/analysis-core/src/heuristics/gap-in-heartbeats.ts) · [tests](../packages/analysis-core/src/heuristics/gap-in-heartbeats.test.ts)                         |
| `multiple_sessions_overlap` | high     | Two sessions in the bundle claim the same wall-clock window. Possible second VS Code instance running, possible session-forgery. A session with no `session.end` (the ordinary crash/kill signature) is bounded at its **last recorded event**, not left open-ended — otherwise one crash flags every later session for the rest of the assignment.                                                                    | [`multiple-sessions-overlap.ts`](../packages/analysis-core/src/heuristics/multiple-sessions-overlap.ts) · [tests](../packages/analysis-core/src/heuristics/multiple-sessions-overlap.test.ts) |
| `extension_hash_mismatch`   | medium   | The recorder's `extension_hash` in the bundle manifest isn't in the course-maintained known-good list ([`config/known-good-extension-hashes.json`](../packages/analysis-core/src/heuristics/config/known-good-extension-hashes.json)). Refresh that list with `npm run update-hashes -- --keypair <path>` after every recorder release — see the [repo root README](../README.md#course-staff-key--manifest-workflow). | [`extension-hash-mismatch.ts`](../packages/analysis-core/src/heuristics/extension-hash-mismatch.ts) · [tests](../packages/analysis-core/src/heuristics/extension-hash-mismatch.test.ts)       |

## Bundle-validation flags

Surfaced from the PRD §5.4 bundle validation checks as flags in the same dashboard. The validation report is produced by [`run-validation.ts`](../packages/analysis-core/src/validation/run-validation.ts); the adapter that lifts failed checks into the flag list lives in [`integrity-flags.ts`](../packages/analysis-core/src/heuristics/integrity-flags.ts).

| Heuristic                   | Severity | Detects                                                                                                                                                                | Source check                                                                                      |
| --------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| `manifest_sig_invalid`      | high     | The `.provenance-manifest` manifest signature didn't verify against the course public key.                                                                             | [`verify-manifest-sig.ts`](../packages/analysis-core/src/validation/verify-manifest-sig.ts)       |
| `session_binding_invalid`   | high     | The session keypair isn't bound to the manifest signature (replay-attack signal — see PRD §6).                                                                         | [`verify-session-binding.ts`](../packages/analysis-core/src/validation/verify-session-binding.ts) |
| `chain_broken`              | high     | Hash chain failed validation at some seq — tampering, corruption, or recorder crash.                                                                                   | [`verify-chain.ts`](../packages/analysis-core/src/validation/verify-chain.ts)                     |
| `monotonic_t_regression`    | medium   | Session-relative `t` field went backwards.                                                                                                                             | [`verify-monotonic-t.ts`](../packages/analysis-core/src/validation/verify-monotonic-t.ts)         |
| `monotonic_wall_regression` | medium   | Wall-clock timestamp went backwards beyond what `clock.skew` events explain.                                                                                           | [`verify-monotonic-wall.ts`](../packages/analysis-core/src/validation/verify-monotonic-wall.ts)   |
| `submitted_code_match`      | high     | The submitted file's bytes don't match the recorder's last recorded on-disk state (1.1+ bundles); tamper sub-check is gated on submitted bytes actually being present. | [`verify-submitted-code.ts`](../packages/analysis-core/src/validation/verify-submitted-code.ts)   |

A seventh check, [`verify-doc-save-hashes.ts`](../packages/analysis-core/src/validation/verify-doc-save-hashes.ts), produces an informational note in the validation report when a save can't be reconstructed (e.g. content over the recorder's inline cap — 64 KB as of recorder 1.1.2; a 4 KB cap applied to `paste`/`fs.external_change` payloads only through earlier 1.1.x builds), but does not produce a flag.

### Bundle-level tamper detections

These three are **not** among the PRD §5.4 eight validation checks. The eight are a frozen, persisted contract — the server stores one `check_N_status` column per check and asserts `checks.length === 8` at ingest — so nothing new is ever added to that list. These detections instead run straight off the bundle inside [`runValidation`](../packages/analysis-core/src/validation/run-validation.ts) and arrive on `ValidationReport.bundleDetections`, a separate array alongside `checks`. From there they route through the same [`integrity-flags.ts`](../packages/analysis-core/src/heuristics/integrity-flags.ts) adapter as the six above, so they end up as ordinary `Flag` rows: same shape, same weight/enable controls in the tuning UI, same ranked queue.

What they add is that they read the bundle's **files**, not its parsed event stream. That is the gap the eight leave: a well-formed entry appended to a sealed `.slog` re-chains cleanly under check 3, keeps `seq` contiguous for check 4, keeps `t`/`wall` non-decreasing for checks 5–6, and touches no file for checks 7–8. All three are severity `high` at confidence 1.0 because each is a cryptographic contradiction rather than a behavioural inference — there is no threshold to tune and no benign reading. Crucially, every "cannot evaluate" path in the three verifiers returns `skipped`, never `fail` (no checkpoints, no session pubkey, a missing digest, absent bytes), so an innocent bundle is never flagged; a flag here exists only because the evidence was present and contradicted itself. The one exception is `checkpoint_chain_valid`'s tail-position `seq_absent` case (see the table below), which genuinely does have a benign reading and is downgraded per-occurrence rather than in the severity table.

| Heuristic                | Severity | Detects                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Source check                                                                                            |
| ------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `log_bytes_match`        | high     | A session's `.slog` or `.slog.meta` bytes don't hash to the `slog_sha256` / `meta_sha256` digest the **signed** bundle manifest commits to — the log was appended to, truncated, or byte-edited after sealing. The only detection that catches a trailing append.                                                                                                                                                                                                                                                                                                                                                                                                                                             | [`verify-log-bytes.ts`](../packages/analysis-core/src/validation/verify-log-bytes.ts)                   |
| `checkpoint_chain_valid` | high\*   | A signed PRD §4.6 `(seq, hash)` checkpoint in `.slog.meta` fails ed25519 verification against the session's own pubkey, names a seq the log no longer holds, or names a hash the chain does not actually hold at that seq. Catches rewriting of a checkpointed prefix, and truncation below the last checkpoint. \*Downgraded to `low`/0.5 when the absent seq is at the very tail of its session (the checkpoint names that session's highest seq, and nothing later exists either) — an honest crash between the checkpoint being signed and the entry being flushed produces that exact shape, indistinguishable from truncation, so that one case is reported as a dual reading instead of an accusation. | [`verify-checkpoint-chain.ts`](../packages/analysis-core/src/validation/verify-checkpoint-chain.ts)     |
| `manifest_downgrade`     | high     | A sub-2.0 embedded assignment manifest carrying Manifest 2.0-only fields (`course_cert`, `course_id`, `collaboration`, `submission`, `scope`, `policy`), which no 1.x signer emits. The manifest was edited after signing — the edit grants nothing, but the attempt is legible.                                                                                                                                                                                                                                                                                                                                                                                                                              | [`verify-manifest-downgrade.ts`](../packages/analysis-core/src/validation/verify-manifest-downgrade.ts) |

## Cross-submission heuristics (Phase 18)

Only run when staff load multiple bundles into the [`/compare` view](../packages/analyzer/src/views/compare/CompareView.tsx). The orchestrator is [`run-cross-heuristics.ts`](../packages/analysis-core/src/heuristics/cross/run-cross-heuristics.ts); cross-flag types are in [`cross/types.ts`](../packages/analysis-core/src/heuristics/cross/types.ts).

| Heuristic                      | Severity      | Detects                                                                                                                        | Source                                                                                                                                                                                                                   |
| ------------------------------ | ------------- | ------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `paste_shared_across_students` | medium / high | Identical pasted text (high, sha256 match) or near-identical text (medium, fuzzy line match) in two or more students' bundles. | [`cross/paste-shared-across-students.ts`](../packages/analysis-core/src/heuristics/cross/paste-shared-across-students.ts) · [tests](../packages/analysis-core/src/heuristics/cross/paste-shared-across-students.test.ts) |
| `editing_pattern_clone`        | medium        | Two students' event sequences are anomalously similar in timing and file-switch order.                                         | [`cross/editing-pattern-clone.ts`](../packages/analysis-core/src/heuristics/cross/editing-pattern-clone.ts) · [tests](../packages/analysis-core/src/heuristics/cross/editing-pattern-clone.test.ts)                      |

## Group work: which heuristics can name a person

A submission may be GROUP work — two or more students sharing one repository, each identified by
a verified enrolment chain ([`identity/resolve-contributors.ts`](../packages/analysis-core/src/identity/resolve-contributors.ts)).
Heuristics still run **once over the whole scope**, and then each resulting flag is charged to a
contributor — or to nobody — by
[`attribute-flags.ts`](../packages/server/src/services/contributors/attribute-flags.ts). A flag
names a person only when ALL of its supporting evidence sits in ONE session AND that session's
enrolment chain verified; everything else stays scope-level, visible at full severity but with no
name attached.

Running the heuristics **per contributor** — a separate pass over each partner's events — was
analysed and closed as out-of-scope (decision D14). The three-way classification below is why,
and it is pinned as an executable test in
[`contributor-scope-boundary.test.ts`](../packages/analysis-core/src/heuristics/contributor-scope-boundary.test.ts).

### Already effectively per-contributor (8)

Their evidence never leaves one session, so `flags.session_id` is populated and the flag is
already charged to the acting student:

`no_intermediate_errors` · `ai_extension_active` · `shell_integration_disabled` ·
`extension_set_changed_mid_assignment` · `clock_jumps` · `gap_in_heartbeats` ·
`paste_matches_known_source` · `large_paste` (its OUTPUT — one flag per paste event)

### Must stay whole-scope (11 names, 10 of them exclusively)

`large_paste` appears in both lists and is the only one that does: its OUTPUT is already
per-contributor (one flag per paste event), while its INPUT is not — the deletion ledger that
classifies a cut-and-paste as an `internal_move` is built from the whole-scope replay, so a
partner's typed code is what keeps the move at `info`. Seven exclusively-scoped + ten
exclusively-whole-scope + `large_paste` on both sides is the full eighteen.

Narrowing the event window to one contributor turns honest pair work into a finding against a
named student. Three distinct mechanisms:

- **The reconstruction gate needs both partners in scope.**
  [`reconstruction-gate.ts`](../packages/analysis-core/src/heuristics/reconstruction-gate.ts)
  skips a file whose content cannot be established, which happens exactly when its events span
  two verified contributors. With one contributor in scope the file reconstructs, and the skip
  becomes a flag: `low_typing_high_output`, `paste_is_solution`, `time_to_first_save_anomaly`,
  `idle_then_complete`, `mass_external_replacement`.
- **The content classification needs the partner's recorded bytes.** `git_merge_in`
  ([`classify-external-changes.ts`](../packages/analysis-core/src/index/classify-external-changes.ts))
  is defined as "these bytes are a provably different verified contributor's recorded work", so
  a `git pull` reads as an out-of-editor edit once that contributor is out of scope:
  `external_edits`, `terminal_active_during_external_change`. The three growth-based heuristics
  above depend on it too, by a second route: `low_typing_high_output`,
  `time_to_first_save_anomaly` and `idle_then_complete` subtract merged-in characters from their
  counts ([`merged-in-content.ts`](../packages/analysis-core/src/heuristics/merged-in-content.ts)),
  so with the partner out of scope the pulled content becomes unexplained output again — the
  reconstruction gate is not the only thing holding them. Two more belong here by a
  different route — `inter_session_external_change` compares CONSECUTIVE sessions and skips a
  cross-contributor pair, so removing the partner's session makes two of one student's sessions
  adjacent; and `large_paste`'s INPUT, because
  [`internal-move.ts`](../packages/analysis-core/src/heuristics/internal-move.ts) reads its
  deletion ledger off the whole-scope replay, so a partner's typed code is what keeps a
  cut-and-paste refactor at `info`.
- **The question is not about one person.** `multiple_sessions_overlap` is defined over PAIRS of
  sessions, and `extension_hash_mismatch` reads the signed manifest rather than any event. For
  these two, a per-contributor scope loses a FACT rather than gaining an accusation: the
  exculpatory "these two partners were working at the same time" statement, and the partner's
  recorder build going unchecked.

### Not applicable (11)

The nine [bundle-validation flags](#bundle-validation-flags) are an adapter over the validation
report and have no event input to scope. The two
[cross-submission heuristics](#cross-submission-heuristics-phase-18) are already pair-level and
already exclude pairs within one scope.

## Things to keep in mind for staff communication

- **None of these are verdicts.** Per PRD §7.4: "the score is never the verdict — it's a sort order for staff triage." Every escalation goes to a human reviewer who verifies via the replay UI.
- **Process evidence over content classification.** The system intentionally does not run any "is this code AI-generated?" classifier. All signals are about _how_ the code came into existence — pastes, external edits, timing — not about the code itself. This is defensible in an academic integrity hearing in a way that statistical AI-detection scores are not (PRD §1, §2 NG5).
- **`paste_matches_known_source` cannot fire today.** Its corpus (leaked solutions, common Stack Overflow answers, or whatever else staff would want to match against) has no upload path or storage anywhere in the system, so it is permanently empty and the heuristic emits 0 flags. The tuning UI marks it disabled and explains why rather than offering a weight that does nothing. The matching mechanism is fully built and tested; only corpus ingestion (a separate feature, PRD §10 Q4) is missing.
- **Cross-submission heuristics require a batch.** They don't run on single-submission review. Staff load multiple bundles into `/compare` to surface `paste_shared_across_students` and `editing_pattern_clone`.
- **Every flag has a "Jump to replay" link.** No claim is shown in the analyzer without a way to verify it against the actual event stream in one click.
- **Configuration is in code.** Default thresholds are in [`config.ts`](../packages/analysis-core/src/heuristics/config.ts). Tuning is staff's call once a labeled sample exists; PRD §10 Q5 flagged this as an open question.
